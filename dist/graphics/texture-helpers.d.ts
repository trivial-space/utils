import { TextureData } from 'tvs-painter';
export declare function getNoiseTextureData({ startX, startY, width, height, data, }: {
    startX?: number;
    startY?: number;
    width: number;
    height: number;
    data?: TextureData;
}): TextureData;
